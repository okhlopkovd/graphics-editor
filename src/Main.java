import graphicseditor.Editor;

public class Main {
    public static void main(String[] args) {
        int windowWidth = 1920;
        int windowHeight = 1280;
        new Editor(windowWidth, windowHeight);
    }
}